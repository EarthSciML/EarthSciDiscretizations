/**
 * Per-family grid accessor modules.
 *
 * Each family lands as its own module (e.g., `./cartesian.ts`) and is
 * re-exported here per `docs/GRIDS_API.md` §2.6.
 */

export type { Dtype, Grid } from "./types.js";
