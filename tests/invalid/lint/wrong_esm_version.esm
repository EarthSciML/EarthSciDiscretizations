{
  "esm": "0.7.0",
  "metadata": {
    "name": "wrong_esm_version",
    "description": "Lint fixture: a library file whose esm version is not the one the ESS schema's $id names must be rejected (L008). The version here is deliberately stale and must NOT be bumped by a corpus-wide version migration -- being wrong is the whole fixture.",
    "tags": ["esd:reproject", "crs:none"]
  },
  "expression_templates": {
    "wrong_esm_version": { "params": ["x"], "body": "x" }
  }
}
