{
  "esm": "1.0.0",
  "metadata": {
    "name": "unlowered_operator_no_rule",
    "description": "Loader fixture: a spatial D on a right-hand side with no discretization rule in scope survives the rewrite fixpoint and must be rejected by the pre-evaluation gate with diagnostic unlowered_operator (esm-spec 9.6.3 constraint 6). Loading alone is permissive; runners must attempt evaluation/compilation."
  },
  "index_sets": {
    "x": { "kind": "interval", "size": 8 }
  },
  "models": {
    "NoRule": {
      "variables": {
        "u": { "type": "unknown", "units": "K", "shape": ["x"], "description": "field with an unlowerable derivative" }
      },
      "equations": [
        {
          "lhs": { "op": "D", "args": ["u"], "wrt": "t" },
          "rhs": { "op": "D", "args": ["u"], "wrt": "x" }
        }
      ]
    }
  }
}
