{
  "category": "simulation",
  "version": "1.0",
  "case": "laplace_beltrami_band_mms",
  "description": "MMS gate: run the inline esm-spec 6.6 tests of problems/laplace_beltrami_band_mms.esm (model LaplaceBeltramiBand, test mms_steady_band_laplace_beltrami) at the problem's default resolution (NLON=144, NLAT=91) through each binding's official simulation pathway. The assertions and tolerances live in the problem file; this manifest only routes the runners.",
  "problem": "../../../../problems/laplace_beltrami_band_mms.esm",
  "model": "LaplaceBeltramiBand",
  "tests": [
    "mms_steady_band_laplace_beltrami"
  ],
  "reference_binding": "julia",
  "bindings_required": [
    "julia",
    "python",
    "rust"
  ],
  "scope_excluded": {
    "go": "rewrite-only port; no makearray lowering / simulator",
    "typescript": "rewrite-only port; no makearray lowering / simulator"
  },
  "integrators": {
    "julia": {
      "algorithm": "Tsit5",
      "reltol": 1e-10,
      "abstol": 1e-12
    },
    "python": {
      "method": "LSODA",
      "rtol": 1e-12,
      "atol": 1e-14,
      "_doc": "Tight LSODA so Python temporal truncation is negligible against the spatial golden."
    },
    "rust": {
      "solver": "Erk",
      "reltol": 1e-10,
      "abstol": 1e-12
    }
  },
  "status": "active",
  "blocked_upstream_bindings": {
    "rust": "Rust hangs at every tolerance tried (loosest reltol 1e-6; all three solvers Erk/Bdf/Sdirk wedge). earthsci-ast-rs's diffsol integrator has no dtmin/max-step fail-fast guard, so on this stiff / non-smooth-min-max / large-AST RHS it drives dt->0 without terminating instead of erroring. Julia (Tsit5) and Python (LSODA) solve it. Remove once the Rust solver gains a step guard."
  }
}
