{
  "category": "simulation",
  "version": "1.0",
  "case": "heat_1d_periodic",
  "description": "MMS gate: run the inline esm-spec 6.6 tests of problems/heat_1d_periodic.esm (model Heat, test mms_periodic_decay) at the problem's default resolution (N=64) through each binding's official simulation pathway. Exercises the second-order periodic Laplacian rule central_D2_periodic on the manufactured mode exp(-alpha (2 pi)^2 t) cos(2 pi x). The assertions and their tolerances live in the problem file itself; this manifest only routes the runners.",
  "problem": "../../../../problems/heat_1d_periodic.esm",
  "model": "Heat",
  "tests": [
    "mms_periodic_decay"
  ],
  "reference_binding": "julia",
  "bindings_required": [
    "julia",
    "python",
    "rust"
  ],
  "scope_excluded": {
    "go": "rewrite-only port; no makearray lowering / simulator",
    "typescript": "rewrite-only port; no makearray lowering / simulator"
  },
  "integrators": {
    "julia": {
      "algorithm": "Tsit5",
      "reltol": 1e-10,
      "abstol": 1e-12
    },
    "python": {
      "method": "LSODA",
      "rtol": 1e-12,
      "atol": 1e-14
    },
    "rust": {
      "solver": "Erk",
      "reltol": 1e-10,
      "abstol": 1e-12
    }
  },
  "status": "active",
  "cross_binding_overrides": {
    "python": {
      "atol": 1e-10,
      "_doc": "Deterministic LSODA/RK45-vs-Tsit5 integrator-method disagreement on a floor-sensitive time-integrated functional: mms_periodic_decay[2] rel 9.11e-08 / abs 2.89e-12 (ref 3.169901e-05). MEASURED from CI run 33272496703 (2026-08-29); the values are bit-stable across runs and the inline assertions pass at the problem-file tolerances, so only the cross-binding re-evaluation band is widened (10x headroom, decade-rounded). Re-tighten if the bindings ever pin the same integrator."
    }
  }
}
