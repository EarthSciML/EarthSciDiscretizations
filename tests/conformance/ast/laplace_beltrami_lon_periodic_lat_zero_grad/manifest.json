{
  "category": "ast",
  "version": "1.0",
  "case": "laplace_beltrami_lon_periodic_lat_zero_grad",
  "description": "Post-lowering AST conformance for grids/latlon/rules/laplace_beltrami_lon_periodic_lat_zero_grad -- the metric spherical Laplacian (Laplace-Beltrami) on a regional latlon band, matching the bare open-namespace laplacian rewrite-target (like fv_laplacian_cell) and emitting the cos(lat)-weighted nine-region makearray (periodic zonal wrap + zero-flux Neumann band edges). Every binding loads fixture.esm (NLON=6, NLAT=5 at the import edge), runs the section-9.6.3 rewrite fixpoint, and must emit a byte-identical expanded AST: zero apply_expression_template nodes, zero unlowered laplacian ops, zero surviving metaparameter names, free names R_sphere/dlon_deg/dlat_deg/lat0_deg surviving. Golden generated by the Julia reference runner only (scripts/regenerate-goldens.sh).",
  "rule": "../../../../grids/latlon/rules/laplace_beltrami_lon_periodic_lat_zero_grad.esm",
  "grid": "latlon",
  "fixture": "fixture.esm",
  "model": "LaplaceBeltramiAst",
  "bindings": {
    "NLON": 6,
    "NLAT": 5
  },
  "reference_binding": "julia",
  "bindings_required": [
    "julia",
    "python",
    "rust",
    "go",
    "typescript"
  ],
  "golden": "expanded.golden.json",
  "status": "active"
}
