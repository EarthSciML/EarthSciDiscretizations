{
  "category": "ast",
  "version": "1.0",
  "case": "varcoeff_face_laplacian_lev_flux_bc",
  "description": "Post-lowering AST conformance for grids/column_nonuniform_1d/rules/varcoeff_face_laplacian_lev_flux_bc: the compound D(K*D(u,lev),lev) replaced by the rule's makearray (interior interface-coefficient stencil aggregate over [2,7]; prescribed-flux faces at k=1 and k=8 reading the free names kdudz_bot and kdudz_top; every flux the one interface_flux_K_lev template). Every binding loads fixture.esm (NLEV bound to 8 at the import edge), runs the section-9.6.3 rewrite fixpoint, and must emit a byte-identical expanded AST in the esm-spec 9.6.4 reference-preserving form (the rule body's stencil references, the grid geometry templates the observeds zc and dz apply to the 9-interface array ze -- the cartesian_nonuniform_1d templates renamed onto lev by the grid's own import edge -- and the shared mesh/mms_column.esm test-column template behind ze all survive uninlined in the document's template registry), zero unlowered rewrite-target ops, zero surviving metaparameter names, the free-name geometry arrays surviving as index references and the lev_nodes axis folded to size 9. The golden is generated by the Julia reference runner only (scripts/regenerate-goldens.sh) and compared bytewise by the other bindings.",
  "rule": "../../../../grids/column_nonuniform_1d/rules/varcoeff_face_laplacian_lev_flux_bc.esm",
  "grid": "column_nonuniform_1d",
  "fixture": "fixture.esm",
  "model": "VarcoeffFaceLaplacianLevAst",
  "bindings": {
    "NLEV": 8
  },
  "reference_binding": "julia",
  "bindings_required": [
    "julia",
    "python",
    "rust",
    "go",
    "typescript"
  ],
  "golden": "expanded.golden.json",
  "status": "active"
}
