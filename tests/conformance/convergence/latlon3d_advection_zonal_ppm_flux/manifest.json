{
  "category": "convergence",
  "version": "1.0",
  "case": "latlon3d_advection_zonal_ppm_flux",
  "description": "Spatial convergence of the UNLIMITED (centred) CW84 conservative flux-form ZONAL advection operator grids/latlon3d/rules/ppm_flux_D_lon_periodic, driven by problems/latlon3d_advection_zonal_ppm_flux. \n\nTHE ACCURACY CEILING OF THE ZONAL FLUX FAMILY. With no limiter the flux difference of the shared CW84 eq-(1.6) interface interpolant reduces algebraically to a 4th-order zonal derivative on the uniform lon mesh, and on this smooth MMS it attains it cleanly in both norms: median pairwise rate L2 3.99 (pairwise 3.99 / 4.00) and Linf 3.99 (3.99 / 4.00). \n\nWHY THE LADDER STOPS AT 128 (rung dropped 2026-07-23, together with the integrator-key repair below). The sweep formerly carried a fourth rung at NLON = 256; it was measuring the INTEGRATOR, not the operator, and it has been removed rather than have its disagreement absorbed by a widened error_vs_golden band. The evidence is a temporal-error residue that survives even at reltol 1e-11 / abstol 1e-13. Take the pure 4th-order extrapolation of each rung from its predecessor (e/16) and compare it against what the reference binding actually reports: at NLON = 64 / 128 the Julia Linf sits +0.97% / +0.10% above the extrapolation -- ordinary truncation curvature, shrinking as it should -- but at NLON = 256 it sits +6.74% ABOVE it, i.e. the number grew away from the 4th-order line instead of settling onto it. A second, independent integrator confirms which part is spurious: Python (LSODA rtol 1e-12 / atol 1e-14) lands at 1.0362e-8, within 0.09% of the extrapolated 1.0353e-8, while Julia (Tsit5) reports 1.1051e-8. The two bindings agree to 8.6e-8 / 3.3e-6 / 8.5e-6 relative at NLON = 32 / 64 / 128 and then to only 6.7e-2 at NLON = 256 -- a four-order-of-magnitude jump in cross-binding spread at a single rung, which is the signature of a solver artifact and not of a discretization. Roughly 7e-10 of the 1.105e-8 the rung reported was Tsit5's own temporal error riding on a ~1.035e-8 spatial signal. The rung did still PASS the cross-binding gate after the tolerance repair, but only because error_vs_golden_atol = 1e-9 is itself ~9% of the quantity being compared there: the absolute floor, not the measurement, was doing the work. A rung that only agrees because the tolerance is larger than the signal measures nothing, so it is gone. Nothing is lost from the order claim -- the gate takes the MEDIAN pairwise rate, both remaining pairwise rates are 3.99-4.00 in both norms, and the manifest's argument was always that the median does not need that rung. That is what makes this case the reference against which the MONOTONE sibling case latlon3d_advection_zonal_ppm_flux_mono is measured: the two differ ONLY in the rule they import, so the gap between the two measured orders is attributable to the eq (1.10) limiter alone. This operator is NOT upwind-biased and cannot be -- under the unlimited reconstruction the two cells straddling a face agree exactly at it, so there is no donor to choose -- and it OVERSHOOTS at a sharp gradient. It measures a ceiling; it is not the operator to run. \n\nFORCED-STEADY (TRUNCATION) IDIOM, on a MMS that is BIT-FOR-BIT the donor-cell driver's. q*(lambda) = 2 + sin(lambda) is an exact steady state of the forced equation (the state is seeded with its exact CELL MEAN, and src is the exact flux divergence -- for a finite-volume cell simply the difference of the exact face fluxes times the exact spherical weight), so the drift measured at t = 0.1 IS the operator's truncation error and the observed rate is the truncation order. Because latlon3d_advection_zonal_upwind_flux, latlon3d_advection_zonal_ppm_flux and latlon3d_advection_zonal_ppm_flux_mono differ ONLY in the rule they import, the three measured orders are directly comparable and every difference between them is a property of the OPERATOR alone. \n\nTHE SWEEP REFINES NLON ONLY, AND THAT IS THE POINT. NLAT = 5 and NLEV = 4 are pinned at the problem's rule-import edge. The polar metric weights are therefore EXERCISED but not REFINED: at NLAT = 5 the half-polar cap rows carry dphi_lat/dS_lat ~ 5.2 against ~1.0 in the tropics, so the cap rows dominate the Linf norm at every rung -- but because dphi never shrinks, the lat-lon coordinate singularity that stalls the MERIDIONAL operator's pointwise Linf under refinement cannot contaminate the zonal order. What is left is a clean zonal check. The ladder starts at 32 (the same rung the donor-cell zonal sweep starts at), well clear of the NLON >= 7 the seven-column monotone tiling needs. \n\nWHAT THIS GUARDS. The wind U = U0 (0.5 + cos lambda) a(j) b(k) is a declared THREE-DIMENSIONAL face-staggered field on the lon_nodes axis that CHANGES SIGN (westward on the 120-degree arc around lambda = 180), so both branches of the upwind donor selection fire in every row, and the a(j) b(k) modulation means a stencil that mis-indexed U as a column profile would be caught rather than silently agreeing. The rule enforces the periodic wrap ITSELF (cell NLON's east face reuses U[1], never U[NLON+1]), which makes sum_i D exactly 0.0; that is verified out-of-band rather than here, because this manifest measures ORDER, not conservation. \n\nINTEGRATOR CONFIG REPAIR (2026-07-23) -- GOLDEN RESET, NOT A REFRESH. This manifest used to pin its solver under a SINGULAR top-level \"integrator\" key. NO runner reads that key: run-julia.jl reads integrators.julia, run-python.py integrators.python and run-rust.sh integrators.rust, each falling back to its OWN default when its per-binding block is absent. The pinned reltol 1e-11 / abstol 1e-13 therefore never took effect and every committed golden here was in fact produced at the Julia runner's default Tsit5 reltol 1e-10 / abstol 1e-12. That was verified rather than assumed: re-running the sweep with the block deliberately set back to 1e-10 / 1e-12 reproduces the pre-repair golden to the last bit. The key is now spelled integrators.julia, so the documented tolerances bind, and the goldens are regenerated at them through scripts/regenerate-goldens.sh. The golden values consequently MOVE -- that is the tolerance change becoming real, a behavioural correction and not a refresh-to-green. The observed orders are unaffected (the pairwise rates quoted above are the post-repair ones and are unchanged to the precision quoted, except where this manifest says otherwise), because a tolerance that was already far below the spatial truncation error at every retained rung moves only the digits it reaches. The same commit adds the integrators.python block and puts python in bindings_required: the Python binding was never scope-excluded here, and compare-outputs.py gates any binding that produces a record regardless of bindings_required, so python was eligible all along and is now declared and measured rather than left implicit.",
  "problem": "../../../../problems/latlon3d_advection_zonal_ppm_flux.esm",
  "model": "ZonalAdvectionPPMFlux3d",
  "grid": "latlon3d",
  "rule": "../../../../grids/latlon3d/rules/ppm_flux_D_lon_periodic.esm",
  "resolutions": [
    {
      "n": 32,
      "bindings": {
        "NLON": 32
      }
    },
    {
      "n": 64,
      "bindings": {
        "NLON": 64
      }
    },
    {
      "n": 128,
      "bindings": {
        "NLON": 128
      }
    }
  ],
  "assert_time": 0.1,
  "expected_order": 4.0,
  "order_tolerance": 0.15,
  "norms": [
    "L2_error",
    "Linf_error"
  ],
  "integrators": {
    "julia": {
      "algorithm": "Tsit5",
      "reltol": 1e-11,
      "abstol": 1e-13
    },
    "python": {
      "method": "LSODA",
      "rtol": 1e-12,
      "atol": 1e-14
    }
  },
  "reference_binding": "julia",
  "bindings_required": [
    "julia",
    "python"
  ],
  "scope_excluded": {
    "go": "rewrite-only port; no makearray lowering / simulator",
    "typescript": "rewrite-only port; no makearray lowering / simulator"
  },
  "tolerances": {
    "error_vs_golden_rtol": 0.0001,
    "error_vs_golden_atol": 1e-09
  },
  "golden": "golden/errors.json",
  "status": "active"
}
