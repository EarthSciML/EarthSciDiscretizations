var documenterSearchIndex = {"docs":
[{"location":"operators/#Operators","page":"Operators","title":"Operators","text":"","category":"section"},{"location":"operators/#The-closed-authoring-vocabulary","page":"Operators","title":"The closed authoring vocabulary","text":"A discretization rule's replacement MUST be expressible in the §4.2 op vocabulary alone. That vocabulary is closed: every binding (Julia, Python, future host) walks the same AST, and no rule introduces new ops. The vocabulary covers everything an FV scheme needs — including limiters, reconstructions, and weighted-stencil schemes — without any scheme-specific dispatch.\n\nThe full list, summarized from esm-spec.md §4.2 (consult that section for the authoritative form):","category":"section"},{"location":"operators/#PDE-operators-(applies_to.op-and-tendency-expressions)","page":"Operators","title":"PDE operators (applies_to.op and tendency expressions)","text":"Op Fields Meaning\nD wrt Time derivative ∂/∂t\ngrad dim Spatial gradient ∂/∂x\ndiv  Divergence ∇·\nlaplacian  Laplacian ∇²\n\nThese four (plus pointwise math, below) are the PDE-operator alphabet that authors pattern-match against. A rule's applies_to.op MUST be one of them.","category":"section"},{"location":"operators/#Pointwise-math","page":"Operators","title":"Pointwise math","text":"Arithmetic: + (n-ary), - (unary or binary), * (n-ary), / (binary), ^ (binary).\n\nElementary functions: exp, log, log10, sqrt, abs, sign, sin, cos, tan, asin, acos, atan, atan2, min (n-ary, ≥2 args), max (n-ary, ≥2 args), floor, ceil.\n\nmin / max are the canonical encoding of clamp / clip / limiter primitives — clamp(x, lo, hi) is min(hi, max(lo, x)). Reviewers reject fn nodes that re-implement these in disguise.","category":"section"},{"location":"operators/#Conditionals","page":"Operators","title":"Conditionals","text":"ifelse [cond, then, else], >, <, >=, <=, ==, !=, and, or, not. Conditional logic in lowerings (e.g. flux-limiter switches) goes through ifelse; there is no if-statement primitive.","category":"section"},{"location":"operators/#Inline-constants-and-registry-calls","page":"Operators","title":"Inline constants and registry calls","text":"const (literal value), enum (file-local symbolic name), fn (invocation of a closed-registry function — name is a dotted path). Most FV rules need none of these; mention them only when shipping a table of coefficients inline (const) or a registry-level helper.","category":"section"},{"location":"operators/#Array-/-tensor-ops-(replacement-body)","page":"Operators","title":"Array / tensor ops (replacement body)","text":"Op Required fields Meaning\narrayop output_idx, expr Generalized Einstein-notation tensor expression with implicit reductions over non-output indices. (§4.3.1.)\nmakearray regions, values Block assembly from overlapping sub-region assignments; later regions overwrite earlier ones. (§4.3.2.)\nindex — Element / sub-array access. args[0] is the array; args[1..] are index expressions. (§4.3.3.)\nbroadcast fn Element-wise application of scalar op fn. (§4.3.4.)\nreshape shape Reshape the operand to a target shape. (§4.3.5.)\ntranspose optional perm Axis permutation. (§4.3.5.)\nconcat axis Concatenate operand arrays along axis. (§4.3.5.)\n\narrayop is the workhorse: every closed-AST FV lowering in the catalog ultimately resolves to one or more arrayop nodes whose body combines index, arithmetic, and (for nonlinear schemes) ifelse and broadcast.","category":"section"},{"location":"operators/#What-the-vocabulary-excludes","page":"Operators","title":"What the vocabulary excludes","text":"Rules MUST NOT introduce off-spec match keys or scheme-named ops in either applies_to.op or anywhere inside replacement. The following names are forbidden:\n\nForbidden as op Right answer\nadvect match D(q, wrt=t) (or whichever PDE op the scheme discretizes) and lower the advective tendency as an arrayop.\nreconstruct the reconstruction is the body of the arrayop — express the polynomial in +, *, index.\nflux flux assembly is an arrayop whose output_idx ranges over edges and whose body is the numerical-flux formula in pointwise math + ifelse.\nlimit / limiter encode the limiter as min / max / ifelse directly.\nbc:* boundary handling is declared on the domain (esm-spec.md §11.5) and applied by downstream BC rules; no bc:* op exists in the lowered AST.\n\nThe rule of thumb: if the only justification for a new op name is \"this scheme calls it that\", it is the wrong move. Express the math in the existing alphabet. If a scheme genuinely cannot be expressed — bring it to the spec authors before authoring the rule.\n\nThe companion prohibition is on scheme-specific kernels in any host language. Rules ship JSON, not Julia or Python. The reference evaluator (evaluate_arrayop and the ESS walker) implements the §4.2 vocabulary once; rules borrow that implementation by composition.","category":"section"},{"location":"operators/#Worked-sanity-checks","page":"Operators","title":"Worked sanity checks","text":"The closed-AST lowerings produced by the catalog satisfy the same basic invariants any FV operator must — they are still operators on arrays, just authored differently. The following examples evaluate the AST that the rules emit, confirming each invariant holds.","category":"section"},{"location":"operators/#Laplacian-of-a-constant","page":"Operators","title":"Laplacian of a constant","text":"The Laplacian of a constant field is zero everywhere. fv_laplacian is the de-facto numeric reference for the schema-gated covariant Laplacian on the cubed sphere (see discretizations/finite_difference/covariant_laplacian_cubed_sphere.json, which currently declares applicable: false pending ESS multi-axis selectors and metric-tensor bindings).\n\nusing EarthSciDiscretizations\nusing EarthSciDiscretizations: evaluate_arrayop\n\ngrid = CubedSphereGrid(8)\nNc = grid.Nc\n\nphi_const = fill(42.0, 6, Nc, Nc)\nao = fv_laplacian(phi_const, grid)\nlap = evaluate_arrayop(ao)\nprintln(\"Laplacian of constant field:\")\nprintln(\"  Size: $(size(lap))\")\nprintln(\"  Max absolute value: $(maximum(abs.(lap)))\")","category":"section"},{"location":"operators/#Transport-of-a-constant-field","page":"Operators","title":"Transport of a constant field","text":"Advecting a constant field produces zero tendency regardless of the velocity field.\n\nq_const = fill(5.0, 6, Nc, Nc)\ncourant_xi = fill(0.3, 6, Nc, Nc)\ncourant_eta = fill(-0.2, 6, Nc, Nc)\n\nao = transport_2d(q_const, courant_xi, courant_eta, grid)\ntendency = evaluate_arrayop(ao)\nprintln(\"Transport tendency of constant field:\")\nprintln(\"  Size: $(size(tendency))\")\nprintln(\"  Max absolute value: $(maximum(abs.(tendency)))\")","category":"section"},{"location":"operators/#Where-to-read-more","page":"Operators","title":"Where to read more","text":"Finite-Volume Method — how a rule's pattern match and closed arrayop replacement encode an FV operator.\nTutorial: Authoring a rule — end-to-end walkthrough.\nesm-spec.md §4.2 (operator vocabulary), §4.3 (array semantics).","category":"section"},{"location":"grid/#Cubed-Sphere-Grid","page":"Cubed-Sphere Grid","title":"Cubed-Sphere Grid","text":"","category":"section"},{"location":"grid/#Overview","page":"Cubed-Sphere Grid","title":"Overview","text":"The cubed-sphere grid projects the six faces of a cube onto the sphere using the gnomonic (central) projection. This avoids the polar singularities of latitude-longitude grids and provides quasi-uniform resolution globally.\n\nEach of the 6 panels is subdivided into N_c times N_c cells in the local (xi eta) coordinate system, where xi eta in -pi4 pi4.","category":"section"},{"location":"grid/#Grid-Construction","page":"Cubed-Sphere Grid","title":"Grid Construction","text":"using EarthSciDiscretizations\n\n# Create a C8 grid on the unit sphere\ngrid = CubedSphereGrid(8; R=1.0)\n\nprintln(\"Nc = $(grid.Nc), Ng = $(grid.Ng), R = $(grid.R)\")\nprintln(\"Grid arrays:\")\nprintln(\"  lon/lat: $(size(grid.lon))\")\nprintln(\"  area:    $(size(grid.area))\")\nprintln(\"  dx:      $(size(grid.dx))  (U-edge lengths)\")\nprintln(\"  dy:      $(size(grid.dy))  (V-edge lengths)\")\nprintln(\"  dxi:     $(grid.dξ)\")\nprintln(\"  deta:    $(grid.dη)\")","category":"section"},{"location":"grid/#Panel-Connectivity","page":"Cubed-Sphere Grid","title":"Panel Connectivity","text":"The 6 panels are connected as follows: Panel 1 (front), Panel 2 (right), Panel 3 (top/north pole), Panel 4 (back), Panel 5 (left), Panel 6 (bottom/south pole).\n\n# Show connectivity for Panel 1\nfor dir in (West, East, South, North)\n    nb = PANEL_CONNECTIVITY[1][dir]\n    println(\"Panel 1 $dir -> Panel $(nb.neighbor_panel) ($(nb.neighbor_edge), reverse=$(nb.reverse_index))\")\nend\n\n# Verify that all connectivity is self-consistent\nprintln(\"\\nConnectivity valid: \", verify_connectivity(8))","category":"section"},{"location":"grid/#Cell-Area-Statistics","page":"Cubed-Sphere Grid","title":"Cell Area Statistics","text":"using Statistics\n\npanel_areas = [sum(grid.area[p, :, :]) for p in 1:6]\ncell_areas = vec(grid.area)\n\nprintln(\"Total area: $(sum(grid.area))\")\nprintln(\"Expected (4pi): $(4pi)\")\nprintln(\"Panel areas: \", round.(panel_areas; digits=6))\nprintln(\"Min cell area: $(minimum(cell_areas))\")\nprintln(\"Max cell area: $(maximum(cell_areas))\")\nprintln(\"Max/Min ratio: $(maximum(cell_areas) / minimum(cell_areas))\")\nprintln(\"Std/Mean: $(std(cell_areas) / mean(cell_areas))\")","category":"section"},{"location":"grid/#Projection-Functions","page":"Cubed-Sphere Grid","title":"Projection Functions","text":"","category":"section"},{"location":"fv_method/#Finite-Volume-Method","page":"Finite-Volume Method","title":"Finite-Volume Method","text":"","category":"section"},{"location":"fv_method/#Overview","page":"Finite-Volume Method","title":"Overview","text":"EarthSciDiscretizations.jl supplies finite-volume (FV) discretization rules that the EarthSciSerialization (ESS) rewriter applies to PDE equation trees. A rule is a small JSON file with two halves:\n\napplies_to — a pattern match against a §4.2 PDE operator (D, grad, div, laplacian, plus the pointwise-math vocabulary).\nreplacement — a closed arrayop lowering in the §4.2 op vocabulary only (arrayop, broadcast, index, ifelse, +, -, *, /, ^, sqrt, min, max, …).\n\nAuthors do not invent scheme-specific match keys (no advect, reconstruct, flux, limit — those names are forbidden as applies_to.op values) and do not ship scheme-specific kernels in any host language. Every binding (Julia, Python, future host) executes the same closed AST by walking the §4.2 ops it already understands.\n\nThe integral conservation law that motivates the FV method survives this move — it now lives in the structure of the arrayop body rather than in a named coefficient table.","category":"section"},{"location":"fv_method/#The-integral-form","page":"Finite-Volume Method","title":"The integral form","text":"For a vector field mathbfF over a cell with area A and boundary partial A, the divergence theorem reads\n\nint_A nabla cdot mathbfF  dA = oint_partial A mathbfF cdot hatn  ds\n\nA rule that lowers div(F) is therefore expected to produce an arrayop whose body sums signed edge fluxes scaled by edge lengths and the inverse cell area. The rule does not need a custom dispatcher to express this: the + and * and index ops in §4.2 are sufficient.","category":"section"},{"location":"fv_method/#Worked-example:-centered_2nd_uniform","page":"Finite-Volume Method","title":"Worked example: centered_2nd_uniform","text":"The smallest rule in the catalog — centered_2nd_uniform (rule page: docs/content/rules/centered_2nd_uniform.md in the Hugo catalog site), landed in commit 7b26ffd — is the canonical exemplar of the closed-AST lowering pattern. It targets grad(u, dim=x) on a uniform Cartesian axis.","category":"section"},{"location":"fv_method/#(a)-The-PDE-operator","page":"Finite-Volume Method","title":"(a) The PDE operator","text":"The continuous operator is the partial derivative\n\nleft(fracpartial upartial xright)(x)\n\nIn §4.2 it is encoded as {\"op\": \"grad\", \"args\": [\"u\"], \"dim\": \"x\"}.","category":"section"},{"location":"fv_method/#(b)-The-pattern-match","page":"Finite-Volume Method","title":"(b) The pattern match","text":"The rule's applies_to clause matches that op verbatim, with metavariables $u and $x for the operand and axis:\n\n\"applies_to\": {\n  \"op\":   \"grad\",\n  \"args\": [\"$u\"],\n  \"dim\":  \"$x\"\n}\n\nThe matcher fires anywhere in the equation tree where a grad node has the declared shape; $u and $x bind to the concrete field name and axis at rewrite time.","category":"section"},{"location":"fv_method/#(c)-The-closed-arrayop-replacement","page":"Finite-Volume Method","title":"(c) The closed arrayop replacement","text":"The replacement is a single arrayop node whose body is a centered two-point difference:\n\n\"replacement\": {\n  \"op\": \"arrayop\",\n  \"output_idx\": [\"$x\"],\n  \"expr\": {\n    \"op\": \"/\",\n    \"args\": [\n      { \"op\": \"-\", \"args\": [\n        { \"op\": \"index\", \"args\": [\"$u\", { \"op\": \"+\", \"args\": [\"$x\", 1] }] },\n        { \"op\": \"index\", \"args\": [\"$u\", { \"op\": \"-\", \"args\": [\"$x\", 1] }] }\n      ]},\n      { \"op\": \"*\", \"args\": [2, \"dx\"] }\n    ]\n  },\n  \"args\": [\"$u\"]\n}\n\nAfter substitution ($u → u, $x → i), the lowering at each interior cell reduces to\n\nleft(fracpartial upartial xright)_i approx fracu_i+1 - u_i-12Delta x\n\nThere is no coeff table, no stencil[] array, no per-host kernel: the arithmetic that yields the second-order centered difference is visible directly in the AST.","category":"section"},{"location":"fv_method/#Boundary-conditions-live-on-the-domain","page":"Finite-Volume Method","title":"Boundary conditions live on the domain","text":"A rule's replacement is the interior closed form. Boundary handling is declared once per field on the domain's boundary_conditions block (esm-spec.md §11.5: periodic, dirichlet / constant, neumann / zero_gradient, robin) and applied as downstream rewrite rules over concrete indices. The lowered AST does not contain bc:* ops.\n\nDomain BC Index transformation applied to $u[$x ± 1]\nperiodic wrap-around: mod($x ± 1 + N, N) (see the periodic_bc rule)\ndirichlet / constant boundary cell reads the prescribed value\nneumann / zero_gradient mirror in-range neighbor (clamp the index)\nrobin mixed coefficient row at the boundary\n\nThis is the same separation the centered exemplar uses: the rule itself stays BC-agnostic, and a downstream BC rule fires at the boundary cells. Authors of new rules should not embed BC logic in their lowering.","category":"section"},{"location":"fv_method/#What-\"discrete-FV-operator\"-means-in-this-framing","page":"Finite-Volume Method","title":"What \"discrete FV operator\" means in this framing","text":"When this guide talks about a discrete divergence, gradient, or Laplacian, it is shorthand for \"the closed arrayop lowering produced when the matching rule fires against the corresponding §4.2 op\". For example:\n\ngrad(u, dim=x) on a uniform Cartesian axis → centered_2nd_uniform → centered two-point difference (above).\ndiv(F^ξ, F^η) on a cubed-sphere C-grid → a rule that lowers to an arrayop summing edge-flux-times-edge-length and dividing by cell area.\nlaplacian(φ) on the cubed sphere → a rule whose arrayop encodes the 5-point orthogonal stencil plus the cross-metric correction terms (the geometry lives in the body of the AST, not in a separate dispatch table).\n\nIn every case the math — the integral form, the metric tensor, the truncation error — is the motivation for the AST shape; the authoring artifact is just the AST.","category":"section"},{"location":"fv_method/#Where-to-read-more","page":"Finite-Volume Method","title":"Where to read more","text":"Operators — the §4.2 op vocabulary you may use inside a replacement.\nTutorial: Authoring a rule — end-to-end walkthrough.\nesm-spec.md §4.2 (operator vocabulary), §4.3 (array semantics), §11.5 (BC types) for the definitive specification.","category":"section"},{"location":"fv_method/#C-grid-staggering","page":"Finite-Volume Method","title":"C-grid staggering","text":"The Arakawa C-grid staggering places different variables at different locations within each cell. Index symbols in an arrayop lowering are local to the rule, but the runtime resolves their lengths from the operand shapes — which in turn come from the staggering:\n\nLocation Symbol Grid Size Description\nCellCenter (i j) (N_c N_c) Scalar fields (tracer, pressure, temperature)\nUEdge (i+12 j) (N_c+1 N_c) Normal velocity component in xi-direction\nVEdge (i j+12) (N_c N_c+1) Normal velocity component in eta-direction\nCorner (i+12 j+12) (N_c+1 N_c+1) Vorticity, stream function\n\nA rule that produces an edge-quantity output gives an output_idx whose range matches the corresponding edge-staggered grid; one that consumes an edge quantity uses index into an array shaped that way.","category":"section"},{"location":"fv_method/#Ghost-cells","page":"Finite-Volume Method","title":"Ghost cells","text":"Inter-panel communication is handled through ghost cells. Each panel is padded with N_g ghost layers on each side, filled from neighboring panels using the connectivity table and index transformations. This happens at the grid level, before any rule fires; rule lowerings see the ghosted arrays as ordinary inputs.","category":"section"},{"location":"fv_method/#References","page":"Finite-Volume Method","title":"References","text":"The finite-volume methods this package targets are based on the following foundational works. Their algorithms motivate the shape of the AST lowerings; nothing in the rule files reproduces a host-language implementation of them.\n\nLin, S.-J. and R. B. Rood (1996). \"Multidimensional Flux-Form Semi-Lagrangian Transport Schemes.\" Monthly Weather Review, 124(9), 2046–2070. — Dimensionally-split transport.\nColella, P. and P. R. Woodward (1984). \"The Piecewise Parabolic Method (PPM) for gas-dynamical simulations.\" Journal of Computational Physics, 54(1), 174–201. — PPM reconstruction and monotonicity limiter.\nLin, S.-J. (2004). \"A 'Vertically Lagrangian' Finite-Volume Dynamical Core for Global Models.\" Monthly Weather Review, 132(10), 2293–2307. — Vertically Lagrangian FV framework.\nPutman, W. M. and S.-J. Lin (2007). \"Finite-volume transport on various cubed-sphere grids.\" Journal of Computational Physics, 227(1), 55–78. — FV transport on cubed-sphere grids.\nRonchi, C., R. Iacono, and P. S. Paolucci (1996). \"The 'Cubed Sphere': A New Method for the Solution of Partial Differential Equations in Spherical Geometry.\" Journal of Computational Physics, 124(1), 93–114. — Gnomonic cubed-sphere projection and metric tensors.","category":"section"},{"location":"#EarthSciDiscretizations.jl","page":"Home","title":"EarthSciDiscretizations.jl","text":"EarthSciDiscretizations.jl provides finite-volume (FV) discretization of partial differential equations on the cubed-sphere grid. It is designed for global atmospheric and oceanic modeling, where the cubed-sphere avoids the polar singularities of latitude-longitude grids while providing quasi-uniform resolution.\n\nThe transport algorithms are based on the Lin-Rood (1996) dimensionally-split scheme with Piecewise Parabolic Method (PPM) reconstruction (Colella & Woodward 1984), adapted to the cubed-sphere geometry following Putman & Lin (2007). Vertical remapping follows Lin (2004). See Finite-Volume Method for full references.","category":"section"},{"location":"#Features","page":"Home","title":"Features","text":"Cubed-sphere grid construction with gnomonic equidistant projection, automatic metric tensor computation, and panel connectivity\nC-grid staggering for scalar fields (cell centers), velocity components (U/V edges), and corner quantities\nGhost cell management for seamless inter-panel communication\nFinite-volume operators implemented as symbolic ArrayOp expressions: Laplacian, PPM reconstruction, 1D flux, and 2D transport\nDiscretization pipeline built on the JSON rule catalog (discretizations/): rule loading (load_rules), stencil-form → replacement-form lowering (lower_stencil_to_replacement), AST coefficient evaluation (eval_coeff), and initial-condition projection (project_initial_condition)","category":"section"},{"location":"#Quick-Start","page":"Home","title":"Quick Start","text":"using EarthSciDiscretizations\n\n# Create a C16 cubed-sphere grid (16x16 cells per panel, unit sphere)\ngrid = CubedSphereGrid(16; R=1.0)\n\nprintln(\"Grid: $(grid.Nc)x$(grid.Nc) cells per panel, 6 panels\")\nprintln(\"Total cells: $(6 * grid.Nc^2)\")\nprintln(\"Total area: $(total_area(grid))\")\nprintln(\"Expected (4pi): $(4pi)\")\nprintln(\"Relative error: $(abs(total_area(grid) - 4pi) / 4pi)\")","category":"section"},{"location":"#Package-Contents","page":"Home","title":"Package Contents","text":"Finite-Volume Method – mathematical foundations and operator formulas\nCubed-Sphere Grid – grid construction and geometry\nOperators – using FV operators on data\nTutorial: Authoring a rule – end-to-end walkthrough for writing a new discretization rule in the closed-AST lowering pattern","category":"section"},{"location":"tutorial/#Tutorial:-Authoring-a-rule","page":"Tutorial: Authoring a rule","title":"Tutorial: Authoring a rule","text":"This tutorial walks through writing a new discretization rule end-to-end in the closed-AST lowering pattern. The running example is centered_2nd_uniform (rule page: docs/content/rules/centered_2nd_uniform.md in the Hugo catalog site) — the two-point centered finite difference for ∂u/∂x on a uniform Cartesian axis. It is the smallest rule in the catalog that exercises every step, and it is currently the canonical linear exemplar (commit 7b26ffd).\n\nBy the end you will have:\n\nPattern-matched a §4.2 PDE operator with metavariables.\nLowered it to a closed arrayop expression in the §4.2 op vocabulary.\nDelegated boundary handling to the domain's boundary_conditions block.\nValidated the rule with a Layer-A canonical-form fixture.\nSet up the Layer-B convergence sweep, which runs against the closed-AST lowering for supported topologies.","category":"section"},{"location":"tutorial/#Step-1-—-Match-a-§4.2-PDE-operator","page":"Tutorial: Authoring a rule","title":"Step 1 — Match a §4.2 PDE operator","text":"A rule's applies_to clause is a pattern that the ESS rewriter unifies against the equation tree. The op field MUST be one of the §4.2 PDE operators: D, grad, div, laplacian (plus the pointwise-math vocabulary for non-spatial transformations).\n\nDo not invent off-spec match keys. Names like advect, reconstruct, flux, and limit are forbidden as applies_to.op values — those are implementation details of a scheme, not PDE operators. Every advection scheme matches a D (or, in flux form, a div); every reconstruction is part of the arrayop body of the rule that emits the reconstructed array.\n\nFor our example, the operator is the spatial gradient:\n\n\"applies_to\": {\n  \"op\":   \"grad\",\n  \"args\": [\"$u\"],\n  \"dim\":  \"$x\"\n}\n\n$u and $x are pattern metavariables. They bind to the actual field name and axis at rewrite time, so the same rule fires for grad(temperature, dim=x) and grad(salinity, dim=x) alike. If a rule applies to multiple PDE ops, write one rule file per pattern — the catalog favours small, single-purpose rules.","category":"section"},{"location":"tutorial/#Step-2-—-Lower-to-a-closed-arrayop","page":"Tutorial: Authoring a rule","title":"Step 2 — Lower to a closed arrayop","text":"The replacement is a single AST node in the §4.2 op vocabulary. arrayop is the usual top-level shape: it carries an output_idx (symbolic indices spanning the result), an expr (the scalar body evaluated at each index point), and args (the input operands).\n\nFor the centered difference, the body is (u[$x+1] - u[$x-1]) / (2·dx):\n\n\"replacement\": {\n  \"op\": \"arrayop\",\n  \"output_idx\": [\"$x\"],\n  \"expr\": {\n    \"op\": \"/\",\n    \"args\": [\n      { \"op\": \"-\", \"args\": [\n        { \"op\": \"index\", \"args\": [\"$u\", { \"op\": \"+\", \"args\": [\"$x\", 1] }] },\n        { \"op\": \"index\", \"args\": [\"$u\", { \"op\": \"-\", \"args\": [\"$x\", 1] }] }\n      ]},\n      { \"op\": \"*\", \"args\": [2, \"dx\"] }\n    ]\n  },\n  \"args\": [\"$u\"]\n}\n\nA few authoring rules that follow from \"stay in the §4.2 vocabulary\":\n\nNo scheme-specific kernels. Every op in the body is one of the §4.2 ops listed in Operators. If you need a limiter, write min / max / ifelse. If you need a polynomial reconstruction, write + / * / index. Reviewers reject fn nodes that re-implement a clamp under a custom name.\nNo bc:* ops in the body. The lowering is the interior closed form; boundaries come in at Step 3.\nNo host-language code anywhere. Rules ship JSON. The reference evaluator implements §4.2 once; rules compose against it.\n\nLarger rules follow the same shape. A flux-form lowering uses an arrayop whose output_idx ranges over edges; a limiter is a broadcast of min/max/ifelse over operand arrays; a 5-point Laplacian is an arrayop summing five index nodes with the appropriate coefficients in the body. The full op alphabet is in Operators.","category":"section"},{"location":"tutorial/#Step-3-—-Delegate-boundary-conditions-to-the-domain","page":"Tutorial: Authoring a rule","title":"Step 3 — Delegate boundary conditions to the domain","text":"Boundary handling is declared once per field on the domain's boundary_conditions block (esm-spec.md §11.5). The rule itself stays BC-agnostic — its lowering covers only the interior. A separate set of downstream BC rewrite rules consumes the domain's BC list and rewrites the index expressions at the boundary cells:\n\nDomain BC Index transformation applied to $u[$x ± 1]\nperiodic wrap-around: mod($x ± 1 + N, N) (see the periodic_bc rule)\ndirichlet / constant boundary cell reads the prescribed value\nneumann / zero_gradient mirror the in-range neighbor (clamp the index)\nrobin mixed coefficient row at the boundary\n\nConcretely, this means do not write ifelse branches in your arrayop body to special-case the boundary cells. Let the lowering be the one-line interior formula; the BC rewriter will rewrite the index expressions at the boundary at lowering time.","category":"section"},{"location":"tutorial/#Step-4-—-Layer-A-canonical-fixture-(validate-the-JSON)","page":"Tutorial: Authoring a rule","title":"Step 4 — Layer-A canonical fixture (validate the JSON)","text":"Layer A is the canonical-form round-trip: load the rule JSON, walk the AST, re-serialize, and compare. It catches schema violations, metavariable typos, and accidental host-language leakage. Every rule ships at least one Layer-A fixture covering the lowering.\n\nThe Layer-A fixture for our running example lives next to the rule:\n\ndiscretizations/finite_difference/centered_2nd_uniform/fixtures/canonical/\n\nRun the catalog tests locally to exercise it:\n\njulia --project=. -e 'using Pkg; Pkg.test()'\n\nThe catalog tests are the defensive layer — they confirm the rule parses, the metavariables resolve, and the lowering walks cleanly. They should pass before you proceed to Step 5.","category":"section"},{"location":"tutorial/#Step-5-—-Layer-B-convergence-sweep","page":"Tutorial: Authoring a rule","title":"Step 5 — Layer-B convergence sweep","text":"Layer B is an MMS (manufactured-solution) convergence sweep that verifies the rule's empirical order of accuracy on a refinement sequence. Each rule ships an input.esm that names the manufactured solution and a sweep of grid sizes; the harness measures L∞ / L₂ error and fits a slope.\n\nLayer B is active: the walker (test/walk_esd_tests.jl) drives the sweep through the canonical discretize → build_evaluator pipeline for every topology with an implemented runner. The supported set is the walker's _LAYER_B_SUPPORTED_TOPOLOGIES — currently 1d_cartesian_periodic, 1d_vertical_column, 2d_latlon_sphere, and 2d_arakawa_periodic. The fixture declares a registered mms_kind so the runner can construct the manufactured solution; for our running example:\n\n{\n  \"rule\": \"centered_2nd_uniform\",\n  \"manufactured_solution\": \"sin(2*pi*x) on [0,1] periodic; derivative 2*pi*cos(2*pi*x)\",\n  \"sampling\": \"cell_center\",\n  \"grids\": [\n    { \"n\": 16 }, { \"n\": 32 }, { \"n\": 64 }, { \"n\": 128 }\n  ],\n  \"applicable\": true,\n  \"mms_kind\": \"sin_2pi_x_periodic\"\n}\n\napplicable: false (paired with a skip_reason naming the tracked blocker) is the escape hatch for rules whose topology does not yet have a Layer-B runner, or whose acceptance signature isn't a manufactured-solution sweep (index-rewrite BC rules, TVD limiters). The walker surfaces those fixtures as structured SKIPs; once the runner for that topology lands, the fixture flips to applicable: true without other edits. It is not a general escape hatch for \"the convergence test is hard to write\", \"we haven't verified the order yet\", or \"the MMS choice is wrong\". A skip_reason that doesn't name a tracked blocker should not land.","category":"section"},{"location":"tutorial/#Step-6-—-Document-and-link","page":"Tutorial: Authoring a rule","title":"Step 6 — Document and link","text":"Each rule has a doc page under docs/content/rules/<rule>.md. Follow the structure of docs/content/rules/centered_2nd_uniform.md — overview, applies_to and replacement AST, BC handoff table, truncation derivation, convergence figure / status. Cross-link to related rules and to esm-spec.md §4.2 / §11.5 for the definitive operator and BC vocabulary.\n\nThe catalog landing page at docs/content/rules/_index.md advertises the closed-AST lowering pattern as the default. New rules should match its framing; rules predating the migration carry a \"legacy form\" note on their page until they are rewritten.","category":"section"},{"location":"tutorial/#Adapt-to-your-scheme","page":"Tutorial: Authoring a rule","title":"Adapt to your scheme","text":"To author a different rule, swap each piece:\n\nStep 1 — choose the §4.2 PDE op your scheme discretizes. (D for most time-dependent schemes; grad / div / laplacian for spatial operators.)\nStep 2 — write the closed arrayop body. Use index, arithmetic, min / max / ifelse, and (for nonlinear or weighted schemes) broadcast. No new ops.\nStep 3 — leave BC handling to the domain. Do not embed BC switches in the body.\nStep 4 — ship a canonical fixture. Make it pass.\nStep 5 — ship the convergence fixture. Use applicable: false with a named-blocker skip_reason only if a tracked prerequisite genuinely blocks the sweep; otherwise the fixture must be applicable: true and the slope must match the declared accuracy.\nStep 6 — write the doc page in the same shape as centered_2nd_uniform.md and cross-link.\n\nFor a contributor-oriented walk through the surrounding repository infrastructure (paths, CI layers, registration), see the Add a new discretization rule companion tutorial (docs/content/tutorials/add-a-rule/ in the Hugo catalog site) — note that until that tutorial migrates to the new pedagogy it still describes the legacy stencil/coefficient form on some steps.","category":"section"}]
}
