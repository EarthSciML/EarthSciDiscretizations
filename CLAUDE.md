Do not put "Co-Authored-By: Claude ..." in any commit messages. Claude is an LLM and LLMs cannot take responsibility for outputs, therefore they cannot be authors.

.esm files should be authored compositionally. Always factor repeated expressions into expression templates or model components. Avoid the use of scripts to generate .esm files because the results are unlikely to be interpretable by humans.
