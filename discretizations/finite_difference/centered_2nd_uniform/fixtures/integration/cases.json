{
  "comment": "Layer-C integration case for centered_2nd_uniform (2nd-order centered gradient). Converts the truncation-only (Layer-B) coverage into a solved trajectory: the field-level pipeline (build_ode_problem) time-integrates du/dt = -v*grad(u) and verifies O(h^2) spatial convergence against the analytic transported cosine.",
  "cases": [
    {
      "name": "grad_1d_convergence",
      "kind": "esd_field_pipeline",
      "manifest": "grad_1d_convergence.esm"
    }
  ]
}
