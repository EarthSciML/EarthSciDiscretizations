{
  "rule": "periodic_bc",
  "applicable": false,
  "skip_reason": "periodic_bc is an index-rewrite rule (pattern + replacement on an `index` op), not a stencil-coefficient rule. It produces no numeric residual on its own — it only modifies neighbour indexing for stencils that consume it. The convergence-fixture model (manufactured solution + L_inf error sweep) is not applicable. See follow-up bead for an index-rewrite-rule fixture kind."
}
