{
  "rule": "periodic_bc",
  "applicable": false,
  "skip_reason": "periodic_bc is a ghost-cell boundary-condition rule (esd-7mj): it rewrites a synthetic `bc` node into the wrapped opposite-end ghost (index(u, N-1), re-indexed per side to u[N] at xmin and u[1] at xmax by the ess-hjg makearray lowering), exactly like neumann_bc / robin_bc. It produces no numeric residual on its own — it only supplies the boundary ghost the interior stencil consumes — so the convergence-fixture model (manufactured solution + L_inf error sweep) is not applicable. The rule's numeric proof is the Layer-A rewrite byte contract (fixtures/rewrite/) plus the live build_ode_problem per-axis smoke tests in test/test_periodic_bc_rule.jl."
}
