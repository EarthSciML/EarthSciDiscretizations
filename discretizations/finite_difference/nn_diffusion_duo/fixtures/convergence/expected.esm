{
  "rule": "nn_diffusion_duo",
  "metric": "Linf",
  "expected_min_order": 1.9,
  "notes": "Equal-weight nearest-neighbor diffusion on a quasi-uniform triangular icosahedral mesh is theoretically O(h^2) for smooth functions; we require observed min order >= 1.9 across the level-2 (320 cells) → level-3 (1280 cells) → level-4 (5120 cells) sequence. The pre-factor is slightly worse than MPAS because all triangle edges have the same weight (no dv_edge/dc_edge correction). Currently un-runnable — Layer-B is gated behind ESS/esm-bpr (unstructured selector dispatch); paired with input.esm's `applicable: false` marker."
}
