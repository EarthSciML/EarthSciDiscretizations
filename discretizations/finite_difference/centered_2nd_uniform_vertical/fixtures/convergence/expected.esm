{
  "rule": "centered_2nd_uniform_vertical",
  "metric": "Linf",
  "expected_min_order": 1.9,
  "notes": "Centered 2nd-order FD on a uniform vertical (sigma) grid is theoretically O(h^2). The stencil math is identical to the cartesian uniform case with h = 1/nz; we require observed min order >= 1.9 across the 16->32->64->128 sequence to tolerate minor pre-asymptotic drift."
}
