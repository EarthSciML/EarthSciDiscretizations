{
  "rule": "centered_8th_deriv_uniform",
  "metric": "Linf",
  "expected_min_order": 7.0,
  "notes": "Centered 8th-order FD second derivative on uniform Cartesian grid is theoretically O(dx^8); we require observed min order >= 7.0 across the 16->32->64 sequence (2 convergence steps). N>=128 excluded: stencil coefficients (max 14350) amplify roundoff as eps/(h^2*5040), dominating the truncation error past the crossover near N~76."
}
