{
  "rule": "nn_diffusion_mpas",
  "applicable": false,
  "skip_reason": "nn_diffusion_mpas is the first MPAS (unstructured Voronoi) rule. The current ESS MMS harness (apply_stencil_periodic_1d in mms_evaluator.jl) only supports 1D periodic Cartesian stencils with scalar bindings dx/h, so it cannot exercise (a) selectors of kind 'reduction' or 'indirect' (RFC discretization.md §7.2 unstructured materialization), (b) connectivity-table bindings (cells_on_cell, edges_on_cell, n_edges_on_cell) or per-cell/per-edge metric bindings (area_cell, dc_edge, dv_edge), (c) the self-targeting 'indirect' row used to close the algebraic identity ∇²u(c) = Σ_k w_k (u(n_k) − u(c)) via Row 2's arrayop coefficient, or (d) a manufactured solution on the MPAS x1.642 / x1.2562 / x1.10242 mesh ladder (no MPAS-aware MMS registered). Tracked for unblocking by follow-up bead dsc-nc5 (ESS MMS harness: MPAS unstructured). Until that lands, the rule's mathematical claim (O(h^2) on a quasi-uniform Voronoi mesh) cannot be checked by the walker; the structural claims (rule discoverable, two-row stencil shape with one reduction + one indirect-self row, selector kinds, AST coefficients including dv_edge / dc_edge / area_cell and an arrayop diagonal-weight reduction) ARE checked by test/test_rule_catalog.jl."
}
