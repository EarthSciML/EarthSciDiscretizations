{
  "rule": "centered_8th_uniform",
  "metric": "Linf",
  "expected_min_order": 7.0,
  "notes": "Centered 8th-order FD on uniform grid is theoretically O(dx^8); we require observed min order >= 7.0 across the 16->32->64->128 sequence. N=256 excluded: stencil coefficients (max 672) amplify roundoff as eps/(h*840) past the truncation crossover near N~160."
}
