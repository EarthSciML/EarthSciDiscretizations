{
  "comment": "Layer-C integration case for nonlinear_laplacian_uniform (grad(f*grad(u))). Converts the truncation-only (Layer-B) coverage into a solved trajectory via the freeze-and-accumulate pattern: the field-level pipeline (build_ode_problem) integrates a state that accumulates the operator applied to frozen u and f, and verifies O(h^2) spatial convergence against the analytic flux divergence.",
  "cases": [
    {
      "name": "nonlinear_diffusion_accum",
      "kind": "esd_field_pipeline",
      "manifest": "nonlinear_diffusion_accum.esm"
    }
  ]
}
