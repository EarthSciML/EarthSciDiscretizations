{
  "rule": "ppm_reconstruction",
  "applicable": false,
  "skip_reason": "ppm_reconstruction does not yet pass the walker harness. The rule's top-level `stencil` is a 5-cell support pattern with `role` annotations and no `coeff` nodes; the runnable 4-point edge interpolation lives under `edge_value_stencil.stencil`. The current ESS verify_mms_convergence assumes a single linear stencil whose output approximates du/dx, but PPM produces a parabolic reconstruction whose edges approximate u, not du/dx. Layer-B coverage exists at tests/fixtures/ppm_reconstruction/ via test/test_ppm_reconstruction_rule.jl (reaches >= 3.0 asymptotic order). A follow-up bead extends ESS to support reconstruction-style rules; this fixture exists so the walker reports a structured SKIP."
}
