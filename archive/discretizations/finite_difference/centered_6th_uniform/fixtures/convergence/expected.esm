{
  "rule": "centered_6th_uniform",
  "metric": "Linf",
  "expected_min_order": 5.5,
  "notes": "Centered 6th-order FD on uniform grid is theoretically O(dx^6); we require observed min order >= 5.5 across the 32->64->128->256 sequence, allowing for pre-asymptotic drift at lower resolutions."
}
