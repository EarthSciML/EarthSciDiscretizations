{
  "rule": "upwind_1st_nonuniform",
  "metric": "Linf",
  "expected_min_order": 0.9,
  "notes": "First-order upwind FD on a sinusoidally-stretched non-uniform grid is O(dx_max) with pre-asymptotic drift on coarse grids. Stretching amplitude 0.3 keeps dx_max / dx_min < 2; we require observed min order >= 0.9 across the 16->32->64->128 sequence."
}
