{
  "esm": "0.5.0",
  "kind": "grid_discretization_descriptor",
  "metadata": {
    "title": "Cartesian 1-D periodic N=16 (dx=0.0625) — centered_2nd_uniform only",
    "description": "Uniform 1-D periodic domain [0,1], N=16 cells, dx=0.0625. Only the 2nd-order centered gradient rule is bound so there is no rule-conflict with upwind_1st on 'grad'."
  },
  "grids": {
    "domain": {
      "spatial": {
        "x": { "min": 0.0, "max": 1.0, "units": "m", "grid_spacing": 0.0625 }
      },
      "boundary_conditions": [
        { "type": "periodic", "dimensions": ["x"] }
      ]
    }
  },
  "discretizations": {
    "centered_2nd_uniform": { "ref": "../../../centered_2nd_uniform.json" }
  }
}
