{
  "rule": "flux_limiter_superbee",
  "applicable": false,
  "skip_reason": "flux_limiter_superbee is a TVD slope-ratio limiter phi(r) = max(0, min(2r, 1), min(r, 2)) — a scalar AST formula, not a stencil. The rule's acceptance is *monotonicity preservation* (Sweby region, phi(1) = 1, strict TVD on slope-ratio inputs), not asymptotic convergence-order on a manufactured solution. The convergence-fixture model is not the right kind for a limiter. A follow-up bead introduces a `monotonicity/` fixture kind for these rules. Numeric coverage of phi(r) checkpoints and TVD on a smooth+square-wave IC already lives at tests/fixtures/flux_limiter_superbee/."
}
